NEXT_PUBLIC_VERCEL_GIT_COMMIT_SHA=local

# Optional: enable /api/feedback to create GitHub issues.
# Prefer a GitHub App for production. For MVP only, use a fine-grained PAT
# stored in Vercel/local env, never in code.
GITHUB_TOKEN=
REPO_OWNER=yanghu819
REPO_NAME=elden-feedback-game
